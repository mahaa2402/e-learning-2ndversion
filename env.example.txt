# Copy this file to .env and fill in your values
# MongoDB Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority

# JWT Secret Key (use a strong random string in production)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Backend Port
PORT=5000

# AWS S3 Configuration
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
AWS_BUCKET_NAME=your-s3-bucket-name

# Email/SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
FROM_EMAIL=your-email@gmail.com

# Frontend API URL (for production, use your EC2 public IP or domain)
# Example: http://your-ec2-ip:5000 or http://yourdomain.com
REACT_APP_API_URL=http://localhost:5000

# Frontend login URL (used in onboarding emails)
PLATFORM_LOGIN_URL=http://localhost:3000/login

# Course completion notification list (comma-separated)
COMPLETION_NOTIFICATION_NAME=Training Coordinator
COURSE_COMPLETION_EXTRA_RECIPIENTS=ops@example.com,hr@example.com,qa@example.com,manager@example.com
# Legacy single-recipient fallback (optional)
# COMPLETION_NOTIFICATION_EMAIL=training@example.com

